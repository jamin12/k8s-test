apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: pg-gateway
  namespace: db
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 5432
        name: tcp-postgres
        protocol: TCP
      hosts:
        - "*"